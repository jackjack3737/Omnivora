name: omnivora_mobile
description: Omnivora mobile — Masterchaif Engine (Sensory Logic) e UI.
version: 0.1.0

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter
  supabase_flutter: ^2.8.0
  flutter_dotenv: ^5.2.1

# Per flutter_dotenv: copia .env.example in .env e inserisci SUPABASE_URL e SUPABASE_ANON_KEY

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
